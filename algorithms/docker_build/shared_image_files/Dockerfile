# This Dockerfile defines the process of building a Docker image.
# It contains instructions to install the Python runtime environment...
# ...and prepare the application for execution inside a container.

# Clear version without commentary 
FROM python:3-alpine
WORKDIR /workspace
COPY . /workspace
RUN apk add --no-cache build-base linux-headers
RUN pip install --no-cache-dir -r requ.txt
RUN mkdir -p /workspace/results
EXPOSE 8080
ENTRYPOINT ["python", "estimation-π.py"]


# Version with commentary 

# Use a lightweight Python base image
FROM python:3-alpine

# Set the working directory inside the container
WORKDIR /workspace

# Copy all files from the current directory on the host to the container
COPY . /workspace

# Install build tools and Linux headers required for some Python packages
RUN apk add --no-cache build-base linux-headers

# Install Python dependencies from requirements file (Python dependencies)
RUN pip install --no-cache-dir -r requ.txt

# Create a folder for output/results generated by the program
RUN mkdir -p /workspace/results

# Expose port 8080 (or 80 port [root access] example) to allow external access
# (Use port 80 on the host if root privileges are available)
EXPOSE 8080

# Set the entrypoint to run the Python script when the container starts
ENTRYPOINT ["python", "estimation-π.py"]